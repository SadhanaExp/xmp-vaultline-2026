import { ReadyToContract } from "@/components/contract/ready-to-contract";

export const dynamic = "force-dynamic";

export default function HomePage() {
  return <ReadyToContract leadId={null} customerId={null} />;
}
